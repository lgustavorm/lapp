name: lapp
description: App de cronometragem de voltas para qualquer veículo (trackday, kart, moto, etc.)
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.19.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^10.0.0
  google_maps_flutter: ^2.5.0
  csv: ^5.0.2
  share_plus: ^7.2.1
  path_provider: ^2.1.2
  sqflite: ^2.3.0
  path: ^1.8.3
  uuid: ^4.3.3
  intl: ^0.19.0
  firebase_core: ^2.24.2
  firebase_crashlytics: ^3.4.8
  firebase_auth: ^4.15.3
  google_sign_in: ^6.1.6
  permission_handler: ^11.1.0
  sensors_plus: ^4.0.2
  fl_chart: ^0.66.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: "^0.13.1"
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icon/icon.png.jpg"
  min_sdk_android: 21 